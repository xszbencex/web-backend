spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/web
    username: root
    password: admin

  banner:
    location: banner.txt

server:
  port: 8081

security:
  jwt:
    token:
      secret-key: secret-key
      expire-length: 300000
